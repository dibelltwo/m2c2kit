{
  "name": "@m2c2kit/ema-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "rollup -c --configServe --configPort 3010",
    "build": "rimraf dist .rollup.cache && rollup -c --configProd",
    "local": "npm run build && python3 -m http.server 4173"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "16.0.3",
    "rimraf": "6.1.3",
    "rollup": "4.59.0",
    "rollup-plugin-esbuild": "6.2.1",
    "rollup-plugin-serve": "3.0.0",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
